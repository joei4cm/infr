# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Compiled shaders
*.spv

# Models / weights (never commit)
*.gguf
*.safetensors

# Editor / OS
.DS_Store
*.swp
/ref/
target-prof/

# Release artifacts (scripts/docker-release.sh output — `docker save` tarballs
# + SHA256SUMS, never committed)
/dist/
